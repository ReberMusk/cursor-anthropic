{
  "name": "cursor-anthropic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Anthropic /v1/messages gateway backed by a pool of Cursor accounts: priority/round-robin scheduling, rate-limit cooldown + failover, SOCKS/HTTP proxy pool, API-key (sk) auth, and a HeroUI admin panel.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "smoke": "node test/smoke.js",
    "smoke:tools": "node test/tool-sim.mjs",
    "web:dev": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build",
    "build": "npm --prefix web install && npm --prefix web run build",
    "setup": "npm install && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^12.4.1",
    "express": "^5.1.0",
    "ioredis": "^5.4.1",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.5",
    "socks": "^2.8.7"
  }
}
